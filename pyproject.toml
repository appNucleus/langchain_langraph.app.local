[project]
name = "langchain-langraph-app"
version = "0.4.0"
description = "FastAPI + LangGraph app server with live Ollama/MCP inventory, dynamic routing, and multi-query synthesis."
requires-python = ">=3.12"
readme = "README.md"
license = { text = "MIT" }

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = ["tests"]
